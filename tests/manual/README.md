# Manual programs

These programs open interactive windows and may run indefinitely. They are
useful for exploratory testing, but they contain no automated assertions and
are intentionally excluded from `tests/run_tests.py`.

Run them individually, in the foreground, from the repository root.
